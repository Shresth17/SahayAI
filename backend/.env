# MongoDB connection settings
# Replace the placeholders below with your actual credentials and DB name.
# Example for a local MongoDB:
# MONGO_URI=mongodb://<username>:<password>@localhost:27017/<dbname>
# Example for MongoDB Atlas (SRV, recommended for cloud):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

MONGO_URI=
MONGO_URI= 
